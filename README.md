# Nina van Hoorn — Portfolio

`npm install` then `npm run dev`

## Add a project

1. Create `src/content/projects/{category}/{slug}/README.md`
2. Put images in `public/images/projects/{slug}/`
3. Done — it's auto-discovered

## Deploy

Push to `main`. GitHub Actions builds and deploys to Pages.
